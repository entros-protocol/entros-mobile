"use strict";

// This file was autogenerated by some hot garbage in the `uniffi-bindgen-react-native` crate.
// Trust me, you don't want to mess with it!
/* tslint:disable */
/* eslint-disable */
// @ts-nocheck
import nativeModule from "./entros_mopro-ffi.js";
import { AbstractFfiConverterByteArray, FfiConverterArray, FfiConverterArrayBuffer, FfiConverterBool, FfiConverterInt32, FfiConverterMap, FfiConverterOptional, UniffiError, UniffiInternalError, UniffiRustCaller, uniffiCreateFfiConverterString, uniffiCreateRecord, uniffiTypeNameSymbol } from "uniffi-bindgen-react-native";
const uniffiCaller = new UniffiRustCaller(() => ({
  code: 0
}));
const uniffiIsDebug =
// @ts-ignore -- The process global might not be defined
typeof process !== "object" ||
// @ts-ignore -- The process global might not be defined
process?.env?.NODE_ENV !== "production" || false;

// Public interface members begin here.

export function generateCircomProof(zkeyPath, circuitInputs, proofLib) /*throws*/{
  return FfiConverterTypeCircomProofResult.lift(uniffiCaller.rustCallWithError(/*liftError:*/FfiConverterTypeMoproError.lift.bind(FfiConverterTypeMoproError), /*caller:*/callStatus => {
    return nativeModule().ubrn_uniffi_entros_mopro_fn_func_generate_circom_proof(FfiConverterString.lower(zkeyPath), FfiConverterString.lower(circuitInputs), FfiConverterTypeProofLib.lower(proofLib), callStatus);
  }, /*liftString:*/FfiConverterString.lift.bind(FfiConverterString)));
}
export function generateHalo2Proof(srsPath, pkPath, circuitInputs) /*throws*/{
  return FfiConverterTypeHalo2ProofResult.lift(uniffiCaller.rustCallWithError(/*liftError:*/FfiConverterTypeMoproError.lift.bind(FfiConverterTypeMoproError), /*caller:*/callStatus => {
    return nativeModule().ubrn_uniffi_entros_mopro_fn_func_generate_halo2_proof(FfiConverterString.lower(srsPath), FfiConverterString.lower(pkPath), FfiConverterMapStringSequenceString.lower(circuitInputs), callStatus);
  }, /*liftString:*/FfiConverterString.lift.bind(FfiConverterString)));
}
export function generateNoirProof(circuitPath, srsPath, inputs, onChain, vk, lowMemoryMode) /*throws*/{
  return FfiConverterArrayBuffer.lift(uniffiCaller.rustCallWithError(/*liftError:*/FfiConverterTypeMoproError.lift.bind(FfiConverterTypeMoproError), /*caller:*/callStatus => {
    return nativeModule().ubrn_uniffi_entros_mopro_fn_func_generate_noir_proof(FfiConverterString.lower(circuitPath), FfiConverterOptionalString.lower(srsPath), FfiConverterSequenceString.lower(inputs), FfiConverterBool.lower(onChain), FfiConverterArrayBuffer.lower(vk), FfiConverterBool.lower(lowMemoryMode), callStatus);
  }, /*liftString:*/FfiConverterString.lift.bind(FfiConverterString)));
}
export function getNoirVerificationKey(circuitPath, srsPath, onChain, lowMemoryMode) /*throws*/{
  return FfiConverterArrayBuffer.lift(uniffiCaller.rustCallWithError(/*liftError:*/FfiConverterTypeMoproError.lift.bind(FfiConverterTypeMoproError), /*caller:*/callStatus => {
    return nativeModule().ubrn_uniffi_entros_mopro_fn_func_get_noir_verification_key(FfiConverterString.lower(circuitPath), FfiConverterOptionalString.lower(srsPath), FfiConverterBool.lower(onChain), FfiConverterBool.lower(lowMemoryMode), callStatus);
  }, /*liftString:*/FfiConverterString.lift.bind(FfiConverterString)));
}

/**
 * You can also customize the bindings by #[uniffi::export]
 * Reference: https://mozilla.github.io/uniffi-rs/latest/proc_macro/index.html
 */
export function moproHelloWorld() {
  return FfiConverterString.lift(uniffiCaller.rustCall(/*caller:*/callStatus => {
    return nativeModule().ubrn_uniffi_entros_mopro_fn_func_mopro_hello_world(callStatus);
  }, /*liftString:*/FfiConverterString.lift.bind(FfiConverterString)));
}
export function verifyCircomProof(zkeyPath, proofResult, proofLib) /*throws*/{
  return FfiConverterBool.lift(uniffiCaller.rustCallWithError(/*liftError:*/FfiConverterTypeMoproError.lift.bind(FfiConverterTypeMoproError), /*caller:*/callStatus => {
    return nativeModule().ubrn_uniffi_entros_mopro_fn_func_verify_circom_proof(FfiConverterString.lower(zkeyPath), FfiConverterTypeCircomProofResult.lower(proofResult), FfiConverterTypeProofLib.lower(proofLib), callStatus);
  }, /*liftString:*/FfiConverterString.lift.bind(FfiConverterString)));
}
export function verifyHalo2Proof(srsPath, vkPath, proof, publicInput) /*throws*/{
  return FfiConverterBool.lift(uniffiCaller.rustCallWithError(/*liftError:*/FfiConverterTypeMoproError.lift.bind(FfiConverterTypeMoproError), /*caller:*/callStatus => {
    return nativeModule().ubrn_uniffi_entros_mopro_fn_func_verify_halo2_proof(FfiConverterString.lower(srsPath), FfiConverterString.lower(vkPath), FfiConverterArrayBuffer.lower(proof), FfiConverterArrayBuffer.lower(publicInput), callStatus);
  }, /*liftString:*/FfiConverterString.lift.bind(FfiConverterString)));
}
export function verifyNoirProof(circuitPath, proof, onChain, vk, lowMemoryMode) /*throws*/{
  return FfiConverterBool.lift(uniffiCaller.rustCallWithError(/*liftError:*/FfiConverterTypeMoproError.lift.bind(FfiConverterTypeMoproError), /*caller:*/callStatus => {
    return nativeModule().ubrn_uniffi_entros_mopro_fn_func_verify_noir_proof(FfiConverterString.lower(circuitPath), FfiConverterArrayBuffer.lower(proof), FfiConverterBool.lower(onChain), FfiConverterArrayBuffer.lower(vk), FfiConverterBool.lower(lowMemoryMode), callStatus);
  }, /*liftString:*/FfiConverterString.lift.bind(FfiConverterString)));
}

// Hermes (React Native ≥ 0.74) ships TextEncoder and encodeInto, but not
// TextDecoder. For single-string decode (bytesToString), we polyfill via the
// C++ string_from_buffer helper using a duck-typed object matching the
// standard TextDecoder.decode signature. Once Hermes ships a real
// TextDecoder, the `typeof` check will pick it up automatically.
//
// For array-of-strings decode (readStringFromBuffer), we keep a dedicated C++
// helper: the polyfill path (new Uint8Array view + decode) measured ~40%
// slower on getStringArray benchmarks than a direct (buf, offset, length)
// call, due to the per-read view allocation and extra property lookups in
// string_from_buffer.
const stringConverter = (() => {
  const encoder = new TextEncoder();
  const decoder = typeof TextDecoder !== "undefined" ? new TextDecoder() : {
    decode: bytes => nativeModule().ubrn_uniffi_internal_fn_func_ffi__string_from_buffer(bytes, undefined)
  };
  return {
    // Single-string lower() uses the C++ helper — TextEncoder.encode
    // measured ~43% slower on takeString benchmarks.
    stringToBytes: s => nativeModule().ubrn_uniffi_internal_fn_func_ffi__string_to_buffer(s, undefined),
    bytesToString: ab => decoder.decode(ab),
    // Direct C++ call — bypasses uniffiCaller.rustCall() overhead.
    // Matters for N-element arrays.
    stringByteLength: s => nativeModule().ubrn_uniffi_internal_fn_func_ffi__string_to_byte_length(s, undefined),
    // Encode directly into the RustBuffer backing store via
    // TextEncoder.encodeInto — zero intermediate allocation. Replaces
    // the old C++ write_string_into_buffer helper.
    writeStringIntoBuffer: (s, buf, offset) => {
      const view = new Uint8Array(buf.arrayBuffer, offset, buf.arrayBuffer.byteLength - offset);
      return encoder.encodeInto(s, view).written;
    },
    // Dedicated C++ helper — avoids per-read Uint8Array allocation and
    // the double property-lookup in string_from_buffer.
    readStringFromBuffer: (buf, offset, length) => nativeModule().ubrn_uniffi_internal_fn_func_ffi__read_string_from_buffer(buf, offset, length)
  };
})();
const FfiConverterString = uniffiCreateFfiConverterString(stringConverter);
/**
 * Generated factory for {@link G1} record objects.
 */
export const G1 = (() => {
  const defaults = () => ({});
  const create = (() => {
    return uniffiCreateRecord(defaults);
  })();
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults())
  });
})();
const FfiConverterTypeG1 = (() => {
  class FFIConverter extends AbstractFfiConverterByteArray {
    read(from) {
      return {
        x: FfiConverterString.read(from),
        y: FfiConverterString.read(from),
        z: FfiConverterString.read(from)
      };
    }
    write(value, into) {
      FfiConverterString.write(value.x, into);
      FfiConverterString.write(value.y, into);
      FfiConverterString.write(value.z, into);
    }
    allocationSize(value) {
      return FfiConverterString.allocationSize(value.x) + FfiConverterString.allocationSize(value.y) + FfiConverterString.allocationSize(value.z);
    }
  }
  ;
  return new FFIConverter();
})();
/**
 * Generated factory for {@link G2} record objects.
 */
export const G2 = (() => {
  const defaults = () => ({});
  const create = (() => {
    return uniffiCreateRecord(defaults);
  })();
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults())
  });
})();
const FfiConverterTypeG2 = (() => {
  class FFIConverter extends AbstractFfiConverterByteArray {
    read(from) {
      return {
        x: FfiConverterSequenceString.read(from),
        y: FfiConverterSequenceString.read(from),
        z: FfiConverterSequenceString.read(from)
      };
    }
    write(value, into) {
      FfiConverterSequenceString.write(value.x, into);
      FfiConverterSequenceString.write(value.y, into);
      FfiConverterSequenceString.write(value.z, into);
    }
    allocationSize(value) {
      return FfiConverterSequenceString.allocationSize(value.x) + FfiConverterSequenceString.allocationSize(value.y) + FfiConverterSequenceString.allocationSize(value.z);
    }
  }
  ;
  return new FFIConverter();
})();
/**
 * Generated factory for {@link CircomProof} record objects.
 */
export const CircomProof = (() => {
  const defaults = () => ({});
  const create = (() => {
    return uniffiCreateRecord(defaults);
  })();
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults())
  });
})();
const FfiConverterTypeCircomProof = (() => {
  class FFIConverter extends AbstractFfiConverterByteArray {
    read(from) {
      return {
        a: FfiConverterTypeG1.read(from),
        b: FfiConverterTypeG2.read(from),
        c: FfiConverterTypeG1.read(from),
        protocol: FfiConverterString.read(from),
        curve: FfiConverterString.read(from)
      };
    }
    write(value, into) {
      FfiConverterTypeG1.write(value.a, into);
      FfiConverterTypeG2.write(value.b, into);
      FfiConverterTypeG1.write(value.c, into);
      FfiConverterString.write(value.protocol, into);
      FfiConverterString.write(value.curve, into);
    }
    allocationSize(value) {
      return FfiConverterTypeG1.allocationSize(value.a) + FfiConverterTypeG2.allocationSize(value.b) + FfiConverterTypeG1.allocationSize(value.c) + FfiConverterString.allocationSize(value.protocol) + FfiConverterString.allocationSize(value.curve);
    }
  }
  ;
  return new FFIConverter();
})();
/**
 * Generated factory for {@link CircomProofResult} record objects.
 */
export const CircomProofResult = (() => {
  const defaults = () => ({});
  const create = (() => {
    return uniffiCreateRecord(defaults);
  })();
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults())
  });
})();
const FfiConverterTypeCircomProofResult = (() => {
  class FFIConverter extends AbstractFfiConverterByteArray {
    read(from) {
      return {
        proof: FfiConverterTypeCircomProof.read(from),
        inputs: FfiConverterSequenceString.read(from)
      };
    }
    write(value, into) {
      FfiConverterTypeCircomProof.write(value.proof, into);
      FfiConverterSequenceString.write(value.inputs, into);
    }
    allocationSize(value) {
      return FfiConverterTypeCircomProof.allocationSize(value.proof) + FfiConverterSequenceString.allocationSize(value.inputs);
    }
  }
  ;
  return new FFIConverter();
})();
/**
 * Generated factory for {@link Halo2ProofResult} record objects.
 */
export const Halo2ProofResult = (() => {
  const defaults = () => ({});
  const create = (() => {
    return uniffiCreateRecord(defaults);
  })();
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults())
  });
})();
const FfiConverterTypeHalo2ProofResult = (() => {
  class FFIConverter extends AbstractFfiConverterByteArray {
    read(from) {
      return {
        proof: FfiConverterArrayBuffer.read(from),
        inputs: FfiConverterArrayBuffer.read(from)
      };
    }
    write(value, into) {
      FfiConverterArrayBuffer.write(value.proof, into);
      FfiConverterArrayBuffer.write(value.inputs, into);
    }
    allocationSize(value) {
      return FfiConverterArrayBuffer.allocationSize(value.proof) + FfiConverterArrayBuffer.allocationSize(value.inputs);
    }
  }
  ;
  return new FFIConverter();
})();

// Error type: MoproError
export let MoproError_Tags = /*#__PURE__*/function (MoproError_Tags) {
  MoproError_Tags["CircomError"] = "CircomError";
  MoproError_Tags["Halo2Error"] = "Halo2Error";
  MoproError_Tags["NoirError"] = "NoirError";
  return MoproError_Tags;
}({});
export const MoproError = (() => {
  class CircomError_ extends UniffiError {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    [uniffiTypeNameSymbol] = "MoproError";
    tag = MoproError_Tags.CircomError;
    constructor(v0) {
      super("MoproError", "CircomError");
      this.inner = Object.freeze([v0]);
    }
    static new(v0) {
      return new CircomError_(v0);
    }
    static instanceOf(obj) {
      return obj.tag === MoproError_Tags.CircomError;
    }
    static hasInner(obj) {
      return CircomError_.instanceOf(obj);
    }
    static getInner(obj) {
      return obj.inner;
    }
  }
  class Halo2Error_ extends UniffiError {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    [uniffiTypeNameSymbol] = "MoproError";
    tag = MoproError_Tags.Halo2Error;
    constructor(v0) {
      super("MoproError", "Halo2Error");
      this.inner = Object.freeze([v0]);
    }
    static new(v0) {
      return new Halo2Error_(v0);
    }
    static instanceOf(obj) {
      return obj.tag === MoproError_Tags.Halo2Error;
    }
    static hasInner(obj) {
      return Halo2Error_.instanceOf(obj);
    }
    static getInner(obj) {
      return obj.inner;
    }
  }
  class NoirError_ extends UniffiError {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    [uniffiTypeNameSymbol] = "MoproError";
    tag = MoproError_Tags.NoirError;
    constructor(v0) {
      super("MoproError", "NoirError");
      this.inner = Object.freeze([v0]);
    }
    static new(v0) {
      return new NoirError_(v0);
    }
    static instanceOf(obj) {
      return obj.tag === MoproError_Tags.NoirError;
    }
    static hasInner(obj) {
      return NoirError_.instanceOf(obj);
    }
    static getInner(obj) {
      return obj.inner;
    }
  }
  function instanceOf(obj) {
    return obj[uniffiTypeNameSymbol] === "MoproError";
  }
  return Object.freeze({
    instanceOf,
    CircomError: CircomError_,
    Halo2Error: Halo2Error_,
    NoirError: NoirError_
  });
})();
// FfiConverter for enum MoproError
const FfiConverterTypeMoproError = (() => {
  const ordinalConverter = FfiConverterInt32;
  class FFIConverter extends AbstractFfiConverterByteArray {
    read(from) {
      switch (ordinalConverter.read(from)) {
        case 1:
          return new MoproError.CircomError(FfiConverterString.read(from));
        case 2:
          return new MoproError.Halo2Error(FfiConverterString.read(from));
        case 3:
          return new MoproError.NoirError(FfiConverterString.read(from));
        default:
          throw new UniffiInternalError.UnexpectedEnumCase();
      }
    }
    write(value, into) {
      switch (value.tag) {
        case MoproError_Tags.CircomError:
          {
            ordinalConverter.write(1, into);
            const inner = value.inner;
            FfiConverterString.write(inner[0], into);
            return;
          }
        case MoproError_Tags.Halo2Error:
          {
            ordinalConverter.write(2, into);
            const inner = value.inner;
            FfiConverterString.write(inner[0], into);
            return;
          }
        case MoproError_Tags.NoirError:
          {
            ordinalConverter.write(3, into);
            const inner = value.inner;
            FfiConverterString.write(inner[0], into);
            return;
          }
        default:
          // Throwing from here means that MoproError_Tags hasn't matched an ordinal.
          throw new UniffiInternalError.UnexpectedEnumCase();
      }
    }
    allocationSize(value) {
      switch (value.tag) {
        case MoproError_Tags.CircomError:
          {
            const inner = value.inner;
            let size = ordinalConverter.allocationSize(1);
            size += FfiConverterString.allocationSize(inner[0]);
            return size;
          }
        case MoproError_Tags.Halo2Error:
          {
            const inner = value.inner;
            let size = ordinalConverter.allocationSize(2);
            size += FfiConverterString.allocationSize(inner[0]);
            return size;
          }
        case MoproError_Tags.NoirError:
          {
            const inner = value.inner;
            let size = ordinalConverter.allocationSize(3);
            size += FfiConverterString.allocationSize(inner[0]);
            return size;
          }
        default:
          throw new UniffiInternalError.UnexpectedEnumCase();
      }
    }
  }
  return new FFIConverter();
})();
export let ProofLib = /*#__PURE__*/function (ProofLib) {
  ProofLib[ProofLib["Arkworks"] = 0] = "Arkworks";
  ProofLib[ProofLib["Rapidsnark"] = 1] = "Rapidsnark";
  return ProofLib;
}({});
const FfiConverterTypeProofLib = (() => {
  const ordinalConverter = FfiConverterInt32;
  class FFIConverter extends AbstractFfiConverterByteArray {
    read(from) {
      switch (ordinalConverter.read(from)) {
        case 1:
          return ProofLib.Arkworks;
        case 2:
          return ProofLib.Rapidsnark;
        default:
          throw new UniffiInternalError.UnexpectedEnumCase();
      }
    }
    write(value, into) {
      switch (value) {
        case ProofLib.Arkworks:
          return ordinalConverter.write(1, into);
        case ProofLib.Rapidsnark:
          return ordinalConverter.write(2, into);
      }
    }
    allocationSize(value) {
      return ordinalConverter.allocationSize(0);
    }
  }
  return new FFIConverter();
})();

// FfiConverter for Array<string>
const FfiConverterSequenceString = new FfiConverterArray(FfiConverterString);

// FfiConverter for Map<string, Array<string>>
const FfiConverterMapStringSequenceString = new FfiConverterMap(FfiConverterString, FfiConverterSequenceString);

// FfiConverter for string | undefined
const FfiConverterOptionalString = new FfiConverterOptional(FfiConverterString);

/**
 * This should be called before anything else.
 *
 * It is likely that this is being done for you by the library's `index.ts`.
 *
 * It checks versions of uniffi between when the Rust scaffolding was generated
 * and when the bindings were generated.
 *
 * It also initializes the machinery to enable Rust to talk back to Javascript.
 */
function uniffiEnsureInitialized() {
  // Get the bindings contract version from our ComponentInterface
  const bindingsContractVersion = 30;
  // Get the scaffolding contract version by calling the into the dylib
  const scaffoldingContractVersion = nativeModule().ubrn_ffi_entros_mopro_uniffi_contract_version();
  if (bindingsContractVersion !== scaffoldingContractVersion) {
    throw new UniffiInternalError.ContractVersionMismatch(scaffoldingContractVersion, bindingsContractVersion);
  }
  if (nativeModule().ubrn_uniffi_entros_mopro_checksum_func_generate_circom_proof() !== 21866) {
    throw new UniffiInternalError.ApiChecksumMismatch("uniffi_entros_mopro_checksum_func_generate_circom_proof");
  }
  if (nativeModule().ubrn_uniffi_entros_mopro_checksum_func_generate_halo2_proof() !== 10461) {
    throw new UniffiInternalError.ApiChecksumMismatch("uniffi_entros_mopro_checksum_func_generate_halo2_proof");
  }
  if (nativeModule().ubrn_uniffi_entros_mopro_checksum_func_generate_noir_proof() !== 49784) {
    throw new UniffiInternalError.ApiChecksumMismatch("uniffi_entros_mopro_checksum_func_generate_noir_proof");
  }
  if (nativeModule().ubrn_uniffi_entros_mopro_checksum_func_get_noir_verification_key() !== 6404) {
    throw new UniffiInternalError.ApiChecksumMismatch("uniffi_entros_mopro_checksum_func_get_noir_verification_key");
  }
  if (nativeModule().ubrn_uniffi_entros_mopro_checksum_func_mopro_hello_world() !== 11162) {
    throw new UniffiInternalError.ApiChecksumMismatch("uniffi_entros_mopro_checksum_func_mopro_hello_world");
  }
  if (nativeModule().ubrn_uniffi_entros_mopro_checksum_func_verify_circom_proof() !== 10869) {
    throw new UniffiInternalError.ApiChecksumMismatch("uniffi_entros_mopro_checksum_func_verify_circom_proof");
  }
  if (nativeModule().ubrn_uniffi_entros_mopro_checksum_func_verify_halo2_proof() !== 11967) {
    throw new UniffiInternalError.ApiChecksumMismatch("uniffi_entros_mopro_checksum_func_verify_halo2_proof");
  }
  if (nativeModule().ubrn_uniffi_entros_mopro_checksum_func_verify_noir_proof() !== 4044) {
    throw new UniffiInternalError.ApiChecksumMismatch("uniffi_entros_mopro_checksum_func_verify_noir_proof");
  }
}
export default Object.freeze({
  initialize: uniffiEnsureInitialized,
  converters: {
    FfiConverterTypeCircomProof,
    FfiConverterTypeCircomProofResult,
    FfiConverterTypeG1,
    FfiConverterTypeG2,
    FfiConverterTypeHalo2ProofResult,
    FfiConverterTypeMoproError,
    FfiConverterTypeProofLib
  }
});
//# sourceMappingURL=entros_mopro.js.map